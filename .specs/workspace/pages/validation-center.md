# Workspace 校验中心页

## 页面定位

校验中心页负责汇总当前月份的待处理问题、突出当前最优先阻塞项，并把人工修复导向对应页面。页面应更像“异常收件箱”，而不是持续运营的分析工作台。

## 源码与依赖

| 类型 | 位置 |
|------|------|
| 页面组件 | `src/features/workspace/pages/ValidationCenterPage.vue` |
| 共享组件 | `WorkspacePageHeader.vue`、`WorkspaceSurface.vue` |
| 查询接口 | `GET /api/workspace/validation?year=&month=`（校验中心使用完整模式；其他轻量页面可追加 `summaryOnly=true`） |
| 处理接口 | `POST /api/workspace/validation/resolve` |

## 核心交互

- 搜索可按类型、描述、团队、日期和来源过滤问题。
- 页面首屏应先突出最高优先级问题，并仅保留 `阻塞风险` 与 `问题总数` 两项核心摘要。
- 问题列表应支持本地选择，但批量处理提交的是真实 issue ID 集合。
- 只有可处理的问题允许进入批量或单条修复动作。
- 问题列表默认按 `blocking first -> severity -> resolvable` 的优先级排序，避免额外的严重级别队列或多列仪表区。
- 单条问题除“Fix now”外，还应提供“Open related area”入口，把人工处理导向月排班、导入导出、人员、班次或团队页面。
- 成功处理后，应使用服务端返回的新列表和新统计覆盖当前页面，而不是乐观更新局部行。
- 导入类问题应只反映当前仍待处理的导入预检上下文；已应用批次或被新预览替换的旧批次，不应继续污染当前月份的导入问题视图。
- 页面应按问题数量自适应：
  - `0 条问题`：显示空态与简短说明，不展示筛选或批量处理区。
  - `1-3 条问题`：显示优先问题卡与简化后的单列表，不展示筛选与批量处理区。
  - `4 条及以上`：再展示搜索、筛选与批量选择能力。

## 数据边界与约束

- Issue ID 以字符串贯穿前后端，避免大整数精度问题。
- 页面年月来源于共享 workspace 月份上下文，与月排班和导入导出保持一致。
- 导入问题的数据边界应与导入页当前有效批次保持一致，避免同月多次预览后出现跨批次混合结果。
- 不可处理的问题需要明确标注为只读或手工处理，避免误导用户。
- 前端可基于 `severity`、`blocking`、`resolvable` 与 `resolutionKind` 进行本地排序和来源标签映射，但不应伪造服务端不存在的问题属性。

## 维护提示

- 若未来引入更复杂的过滤器、批量策略或修复工作流，应拆分为独立子章节。
- 问题模型、严重级别统计或 resolve 协议变化时，需要同步更新本页和服务端 `validation` spec。
